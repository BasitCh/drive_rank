{
  "name": "drive-rank-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Security-rules tests for firestore.rules, run against the Firestore emulator.",
  "scripts": {
    "test": "mocha --timeout 20000 firestore.rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "mocha": "^10.7.3",
    "firebase": "^10.14.1"
  }
}
